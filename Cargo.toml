[package]
name = "tsh"
version = "0.1.0"
edition = "2024"

[dependencies]
deno_task_shell = { version = "0.33.0", default-features = false }
rquickjs = { version = "0.12", features = ["futures"] }
pollster = "0.4"
